# foot terminal config for aquawm: use a real monospace font so foot never
# falls back to proportional Noto Sans (which triggers its
# "font does not appear to be monospace" warning).
# Install: mkdir -p ~/.config/foot && cp examples/foot.ini ~/.config/foot/foot.ini
font=DejaVu Sans Mono:size=11
